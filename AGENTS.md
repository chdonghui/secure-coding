# AI 및 코딩 에이전트 작업 규칙

이 파일은 저장소 루트와 모든 하위 경로에 적용됩니다. Codex, ChatGPT, Claude 및
기타 코딩 에이전트는 작업을 시작하기 전에 이 문서를 읽고 따라야 합니다.

## 프로젝트 성격

- 이 프로젝트는 [ugonfor/secure-coding](https://github.com/ugonfor/secure-coding)
  기반 코드를 사용한 웹 보안 실습 프로젝트입니다.
- 교육 및 실습 목적이며, 현재 개발 서버와 설정을 그대로 운영 환경에 배포하지
  않습니다.
- 현재 프로젝트 버전은 `6.1`입니다.

## 작업 시작 시 읽을 문서

다음 순서로 현재 상태와 작업 규칙을 확인합니다.

1. `AGENTS.md`: AI 및 코딩 에이전트의 필수 작업 규칙
2. `README.md`: 현재 버전, 개발환경, 설치, 실행 및 테스트 방법
3. `docs/FEATURE_CHANGELOG.md`: 버전별 추가·변경·제거 기능과 사용 흐름
4. `docs/SECURITY_REVIEW.md`: 현재 코드 기준 보안 체크리스트와 남은 작업
5. `docs/SECURITY_CHANGELOG.md`: 버전별 상세 보안 조치와 검증 이력
6. `docs/VERSIONING.md`: 버전 결정, 테스트 및 필수 커밋 절차

문서와 실제 코드가 다르면 코드를 확인해 근거를 확보한 후 관련 문서를 함께
수정합니다. 문서 내용만 믿고 보안 항목을 완료 처리하지 않습니다.

## 문서별 책임

### `README.md`

- 현재 버전과 관련 문서 링크를 제공합니다.
- 개발환경, 설치, 의존성, 실행 및 테스트 방법을 설명합니다.
- 버전별 상세 기능·보안 조치나 긴 보안 체크리스트를 직접 작성하지 않습니다.
- 기능은 `docs/FEATURE_CHANGELOG.md`, 상세 보안 내용은
  `docs/SECURITY_CHANGELOG.md`와 `docs/SECURITY_REVIEW.md`로 연결합니다.

### `docs/FEATURE_CHANGELOG.md`

- 버전별로 새로 추가하거나 변경·제거한 기능을 사용자 관점에서 기록합니다.
- 기능의 목적, 접근 경로, 사용 흐름, 접근 권한, 관련 파일과 제한사항을
  작성합니다.
- 기능 추가가 없는 버전은 보안·운영 개선만 있었음을 명시합니다.
- 최신 버전을 문서 위쪽에 배치하고 실제 구현과 테스트가 확인된 기능만
  기록합니다.
- 보안 구현의 상세 근거는 중복하지 않고
  `docs/SECURITY_CHANGELOG.md`로 연결합니다.

### `docs/SECURITY_CHANGELOG.md`

- 버전별로 실제 적용한 보안 조치와 검증 결과를 기록합니다.
- 최신 버전을 문서 위쪽에 배치합니다.
- 각 버전은 다음 순서를 유지합니다.
  1. 버전 정보
  2. 직전 버전과의 조치 전후 비교
  3. 고유 ID를 가진 상세 적용 내역과 코드 근거
  4. 데이터베이스 변경과 마이그레이션
  5. 템플릿, 의존성 및 환경변수 변경
  6. 테스트 명령, 결과 및 검증 시나리오
  7. 다음 버전에 남은 보안 항목
- 적용하지 않았거나 검증하지 않은 조치를 완료로 기록하지 않습니다.

### `docs/SECURITY_REVIEW.md`

- 현재 코드 전체를 기준으로 적용, 미적용, 부분 적용 및 N/A 상태를 관리합니다.
- 보안 구현을 완료하면 체크박스와 코드 근거를 갱신합니다.
- 상세한 과거 버전 내용은 중복 작성하지 않고
  `docs/SECURITY_CHANGELOG.md`로
  연결합니다.

### `docs/VERSIONING.md`

- Major/Minor 결정 기준, 사용자 요청 Patch 커밋과 버전 커밋 절차의 단일 기준
  문서입니다.
- 버전 변경, 커밋 메시지, 커밋 금지 조건은 이 문서를 따릅니다.

### 세션·계정 간 작업 연속성

- 작업을 시작할 때 반드시 `git status --short`와 현재 변경 diff를 확인하고,
  다른 세션이나 사용자가 만든 변경을 보존합니다.
- 이전 대화의 기억에 의존하지 말고 `README.md`, `docs/SDLC.md`, 기능·보안·버전
  문서와 실제 코드를 기준으로 현재 상태를 다시 확인합니다.
- 기능·권한·DB·UI를 변경하는 동안 관련 문서를 같은 작업 흐름에서 갱신합니다.
  구현만 끝내고 문서를 다음 세션으로 미루지 않습니다.
- 새 기능은 최소한 목적, 사용 경로, 권한, 관련 파일, 테스트 명령·결과,
  알려진 한계를 `docs/FEATURE_CHANGELOG.md`, `docs/SECURITY_REVIEW.md`,
  `docs/SDLC.md`에 반영합니다. 보안 변경은 `docs/SECURITY_CHANGELOG.md`에도
  기록합니다.
- 작업을 완료하거나 중단할 때 현재 완료 항목, 미완료 항목, 실패한 테스트,
  다음 작업을 `docs/SDLC.md`의 유지보수 현황 또는 관련 변경 이력에 남겨
  다른 계정·세션이 바로 이어서 작업할 수 있게 합니다.
- 모든 작업은 코드 변경 여부와 관계없이 종료 전에 관련 Markdown 문서를
  최신 상태로 갱신합니다. 최소한 작업 결과·검증 명령과 결과·미완료 항목을
  `docs/SDLC.md`에 남기고, 기능·보안·버전·설치·테스트에 영향을 주면 각각
  `docs/FEATURE_CHANGELOG.md`, `docs/SECURITY_CHANGELOG.md`,
  `docs/SECURITY_REVIEW.md`, `README.md`, `docs/TESTING.md` 및
  `docs/VERSIONING.md` 중 해당 문서를 같은 작업 흐름에서 갱신합니다.
- Markdown을 갱신하지 못한 경우에는 그 사유와 미갱신 문서를 완료 보고에
  명시하고, 완료 항목으로 표시하지 않습니다.
- 문서와 코드가 충돌하면 코드를 근거로 문서를 수정하고, 확인하지 못한 내용은
  완료로 표시하지 않습니다.

## 버전 변경 규칙

- 최초 기준 버전은 `1.0`입니다.
- 보안 취약점 해결, 기존 기능 개선, 페이지·상세 기능 추가 및 호환 가능한
  소규모 스키마 변경은 Minor Version을 증가시킵니다.
- 핵심 도메인 테이블, 송금·결제, 검색, 관리자 권한 체계와 같은 대형 기능은
  Major Version을 증가시키고 Minor를 `0`으로 초기화합니다.
- 오탈자 수정, 링크 수정, 설명 보완 또는 문서 구조 재배치만 수행한 경우에는
  버전을 올리지 않습니다.
- 사용자가 명시적으로 커밋을 요청한 경우에는 기존 릴리스 버전을 기준으로
  `1.0.1`과 같은 Patch 번호를 커밋 메시지 식별자에 사용할 수 있습니다.
- Patch 번호는 Major.Minor별로 독립적인 연속 번호입니다. `1.0`의 첫 두 요청은
  `1.0.1`, `1.0.2`이고, `1.1`의 첫 두 요청은 `1.1.1`, `1.1.2`입니다.
- 프로젝트 Major.Minor가 변경되면 Patch 번호를 `1`부터 다시 시작합니다.
- 다음 Patch 번호는 현재 Major.Minor의 기존 Patch 커밋 중 가장 큰 번호에 1을
  더합니다. 기존 Patch 커밋이 없으면 `1`을 사용합니다.
- Patch 커밋은 작은 문서·테스트·설정·내부 수정에만 사용하며, 보안 취약점 해결,
  기능 추가 또는 주요 스키마 변경을 Patch로 축소하지 않습니다.
- Patch 번호를 커밋 식별자로만 사용한 경우 README의 현재 릴리스 버전은
  변경하지 않습니다. 사용자가 Patch를 프로젝트 버전으로 올리도록 요청하면
  모든 버전 문서를 함께 갱신합니다.
- 버전 판단이 모호하면 코드 변경 범위와 데이터 호환성을 확인하고
  `docs/VERSIONING.md`를 우선합니다.

## 버전 변경 시 필수 작업

버전이 변경되면 다음 작업을 모두 완료해야 합니다.

1. 구현과 마이그레이션을 완료합니다.
2. 관련 정상·실패·보안 회귀 테스트를 실행합니다.
3. 테스트가 실패하면 버전을 올리거나 커밋하지 않습니다.
4. 다음 문서의 버전을 동일하게 갱신합니다.
   - `AGENTS.md`
   - `README.md`
   - `docs/FEATURE_CHANGELOG.md`
   - `docs/SECURITY_CHANGELOG.md`
   - `docs/SECURITY_REVIEW.md`
   - `docs/VERSIONING.md`
5. 기능을 추가·변경·제거했다면 `docs/FEATURE_CHANGELOG.md`에 목적, 사용
   흐름, 권한, 관련 파일과 제한사항을 기록합니다.
6. `docs/SECURITY_CHANGELOG.md`에 상세 조치와 테스트 근거를 추가합니다.
7. `docs/SECURITY_REVIEW.md`의 현재 상태와 요약 변경 이력을 갱신합니다.
8. `git diff --check`, `git diff`, `git status --short`로 변경 범위와
   비밀정보 포함 여부를 확인합니다.
9. 사용자가 커밋하지 말라고 명시하지 않았다면 해당 버전에 속한 파일만
   스테이징하여 버전 커밋을 생성합니다.

Major/Minor 릴리스 커밋 메시지는 다음 형식을 사용합니다.

```text
release: v<Major.Minor> <핵심 변경 요약>
```

사용자가 명시적으로 요청한 작은 변경의 Patch 커밋은 다음 형식을 사용할 수
있습니다.

```text
commit: v<Major.Minor.Patch> <핵심 변경 요약>
```

버전 커밋을 만들었다는 이유만으로 자동 Push, Git 태그, GitHub Release 또는
배포를 수행하지 않습니다. 이 작업들은 사용자의 별도 요청이 필요합니다.

## 보안 구현 규칙

- 사용자 입력은 클라이언트 속성에 의존하지 않고 서버에서 검증합니다.
- 비밀번호는 평문으로 저장하거나 비교하지 않습니다.
- 상태 변경 요청에는 CSRF 보호와 적절한 HTTP 메서드를 적용합니다.
- 인증뿐 아니라 대상 리소스의 소유권과 권한을 서버에서 검증합니다.
- HTML, JavaScript, URL 등 출력 문맥에 맞는 인코딩을 사용합니다.
- SQL 사용자 입력은 파라미터 바인딩을 사용합니다.
- 오류 응답에서 Stack Trace, DB 정보, 비밀키와 내부 구현을 노출하지 않습니다.
- 새 보안 조치는 정상 요청과 공격성 요청을 모두 포함하는 테스트로 검증합니다.
- 범위 밖이거나 미완료인 보안 항목은 숨기지 않고 남은 작업으로 기록합니다.

## 비밀정보와 데이터베이스

- 비밀번호, 세션 비밀키, API 토큰, ngrok 인증 토큰 및 실제 개인정보를 코드,
  문서, 테스트 출력 또는 Git에 추가하지 않습니다.
- 예제 환경변수는 실제 비밀값이 아닌 명확한 플레이스홀더를 사용합니다.
- 데이터베이스 마이그레이션 전에 필요한 백업은 저장소 밖에 만들고 권한을
  제한합니다.
- 평문 비밀번호가 포함된 DB, 백업, 로그 또는 테스트 산출물을 커밋하지
  않습니다.
- 캐시, 바이트코드, 임시 파일 및 테스트용 DB를 버전 파일에 포함하지 않습니다.

## 테스트와 검증

프로젝트의 권장 환경은 `secure_coding` Conda 환경입니다.

```sh
conda activate secure_coding
python -m pytest -q
git diff --check
```

애플리케이션을 직접 실행하거나 import하는 검증에는 32자 이상의 테스트용
`MARKET_SECRET_KEY`가 필요합니다. 실제 운영 비밀키를 테스트에 사용하지 않습니다.

보안 체크리스트를 완료로 변경하려면 다음 조건을 만족해야 합니다.

- 서버 보안 조치가 실제 코드에 존재합니다.
- 우회 요청과 실패 경로가 테스트되었습니다.
- 테스트가 통과합니다.
- 코드 근거와 테스트 결과가 관련 문서에 기록되었습니다.
- 남아 있는 제한사항이 명확하게 기록되었습니다.

## Git 작업 규칙

- 사용자가 만든 기존 변경을 보존하고 관계없는 파일을 수정하지 않습니다.
- 스테이징 시 `git add .` 대신 해당 버전에 속한 파일을 명시합니다.
- 비밀정보와 실제 데이터가 없는지 스테이징 전에 다시 확인합니다.
- 테스트 실패, 문서 버전 불일치 또는 비밀정보 의심 시 커밋하지 않습니다.
- 문서만 수정하고 버전이 변경되지 않았다면 버전 커밋을 생성하지 않습니다.
- 단, 사용자가 문서 등 작은 변경의 커밋을 명시적으로 요청하면
  `Major.Minor.Patch` 형식의 커밋 식별자를 사용할 수 있습니다.
- 강제 Push, 기록 재작성 및 파괴적인 Git 명령을 사용하지 않습니다.

## 문서 검증

Markdown 문서를 수정한 경우 최소한 다음을 확인합니다.

```sh
git diff --check
```

- README에는 현재 버전과 기능·보안 문서 링크가 올바르게 연결되어 있어야 합니다.
- Markdown 코드 블록은 열고 닫는 구분자가 일치해야 합니다.
- 로컬 문서 링크의 대상 파일이 존재해야 합니다.
- 스크린샷 파일이 저장소에 없다면 깨진 이미지 링크를 추가하지 않습니다.

작업 종료 전에는 변경된 Markdown 문서의 링크·코드 블록·버전·테스트 결과를
실제 코드와 대조하고 `git diff --check`, `git diff`, `git status --short`를
실행합니다.
