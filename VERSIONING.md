# 버전 및 커밋 관리 규칙

이 문서는 프로젝트 버전이 변경될 때 코딩 에이전트와 개발자가 따라야 하는
버전 결정, 검증 및 Git 커밋 절차를 정의합니다.

## 현재 버전

`1.2`

현재 버전은 다음 문서에서 동일하게 유지해야 합니다.

- `README.md`의 현재 버전
- `SECURITY_CHANGELOG.md`의 최신 버전과 버전 빠른 보기
- `SECURITY_REVIEW.md`의 `Current Version`
- 이 문서의 현재 버전
- `SECURITY_REVIEW.md`의 변경 이력

## 버전 형식

프로젝트 릴리스 버전은 기본적으로 `Major.Minor` 형식을 사용합니다. 사용자가
명시적으로 커밋을 요청한 경우에는 커밋 식별자에 한해
`Major.Minor.Patch` 형식을 사용할 수 있습니다.

### 최초 버전

- 프로젝트의 최초 버전은 `1.0`입니다.

### Minor Version

기존 프로젝트를 개선하거나 기존 구조와 호환되는 기능을 추가할 때 Minor 숫자를
1씩 증가시킵니다.

예시:

- 보안 취약점 해결: `1.0` → `1.1`
- 기존 데이터베이스 컬럼·인덱스·제약조건 보완: `1.1` → `1.2`
- 페이지 또는 상세 기능 추가: `2.0` → `2.1`
- 기존 기능의 입력 검증, 권한 확인, 감사 로그 추가

### Major Version

핵심 도메인이나 서비스 구조가 크게 확장될 때 Major 숫자를 1 증가시키고 Minor를
`0`으로 초기화합니다.

예시:

- 핵심 데이터베이스 스키마 또는 주요 업무 테이블 추가
- 송금·결제 기능 추가
- 상품 검색 기능 추가
- 관리자 페이지와 관리자 권한 체계 추가
- `1.x`에서 큰 기능 추가: `2.0`
- `2.x`에서 큰 기능 추가: `3.0`

기존 기능을 보완하는 소규모 스키마 변경은 Minor, 새로운 핵심 업무 데이터를
도입하는 스키마 변경은 Major로 판단합니다.

### 사용자 요청 커밋의 Patch 번호

사용자가 “커밋해줘”와 같이 커밋 생성을 명시적으로 요청한 경우, Major 또는 Minor
릴리스가 필요하지 않은 작은 변경은 `1.0.1`과 같은 Patch 번호를 커밋 메시지에
사용할 수 있습니다.

- Patch 번호는 사용자가 명시적으로 커밋을 요청한 경우에만 사용합니다.
- 오탈자, 문서, 링크, 테스트, 설정 또는 작은 내부 수정처럼 기존 기능과
  호환되는 변경에 사용할 수 있습니다.
- 보안 취약점 해결, 기능 추가, 주요 스키마 변경을 Patch로 축소하지 않습니다.
  이런 변경은 기존 Major/Minor 기준을 따릅니다.
- Patch 번호는 현재 Major.Minor 버전별로 독립적으로 관리합니다.
- 현재 `1.0`에서 사용자가 처음 커밋을 요청하면 `1.0.1`, 다시 요청하면
  `1.0.2`를 사용합니다.
- 현재 `1.1`에서 사용자가 처음 커밋을 요청하면 `1.1.1`, 프로젝트 릴리스
  버전이 그대로인 상태에서 다시 요청하면 `1.1.2`를 사용합니다.
- Major 또는 Minor 버전이 바뀌면 새 기준 버전의 Patch 번호를 `1`부터 다시
  시작합니다. 예를 들어 `1.0.4` 이후 프로젝트가 `1.1`로 올라가면 다음 사용자
  요청 Patch 커밋은 `1.1.1`입니다.
- 다음 Patch 번호는 현재 Major.Minor에 해당하는 기존 커밋 메시지 중 가장 큰
  Patch 번호에 1을 더해 결정합니다. 기존 Patch 커밋이 없으면 `1`을 사용합니다.
- 동일한 Patch 번호를 Git 이력에서 다시 사용하지 않습니다.
- 사용자가 커밋을 요청하지 않았다면 문서 수정만으로 Patch 번호를 만들거나
  자동 커밋하지 않습니다.

Patch 번호를 커밋 식별자로만 사용하면 README의 현재 릴리스 버전은 변경하지
않습니다. 사용자가 프로젝트 버전 자체를 `1.0.1`로 올리도록 요청한 경우에는
릴리스 버전 변경으로 간주하고 모든 버전 문서와 변경 이력을 함께 갱신합니다.

## 버전 변경 시 필수 커밋 규칙

버전 번호를 변경한 작업은 검증 완료 후 반드시 하나의 버전 커밋으로 남깁니다.
코딩 에이전트는 사용자가 커밋하지 말라고 명시하지 않는 한 다음 절차를
완료해야 합니다.

1. 변경 규모에 맞는 새 버전을 결정합니다.
2. 코드, 템플릿, 스키마 또는 마이그레이션을 구현합니다.
3. 정상 동작, 보안 및 회귀 테스트를 실행합니다.
4. 테스트가 실패하면 버전 번호를 올리거나 커밋하지 않습니다.
5. `README.md`, `SECURITY_CHANGELOG.md`, `SECURITY_REVIEW.md`,
   `VERSIONING.md`의 버전을 동일하게 갱신합니다.
6. `SECURITY_CHANGELOG.md`에 상세 보안 조치와 테스트 근거를 추가하고,
   `SECURITY_REVIEW.md` 변경 이력에는 요약을 추가합니다.
7. `git status`와 `git diff`로 버전 범위 밖의 파일이나 비밀정보가 없는지
   검토합니다.
8. 해당 버전에 속하는 파일만 스테이징합니다.
9. 아래 형식으로 버전 커밋을 생성합니다.
10. 커밋 후 `git status`와 커밋 내용을 다시 확인합니다.

버전 번호만 단독으로 먼저 커밋하거나, 구현과 문서를 서로 다른 버전 커밋으로
분리하지 않습니다. 한 버전의 구현, 테스트, 마이그레이션 및 문서는 같은 버전
커밋에 포함합니다.

## 커밋 메시지 형식

Major/Minor 릴리스 커밋:

```text
release: v<Major.Minor> <핵심 변경 요약>
```

사용자가 명시적으로 요청한 Patch 식별자 커밋:

```text
commit: v<Major.Minor.Patch> <핵심 변경 요약>
```

예시:

```text
release: v1.1 회원 및 프로필 보안 강화
release: v1.2 상품 수정·삭제와 소유자 검증 추가
release: v2.0 송금 기능과 거래 스키마 추가
commit: v1.1.1 README와 에이전트 규칙 정리
commit: v1.1.2 설치 문서 설명 보완
```

커밋 본문에는 필요한 경우 다음 내용을 기록합니다.

```text
- 주요 구현 내용
- 데이터베이스 또는 마이그레이션 변경
- 실행한 테스트와 결과
- 배포·환경변수·호환성 주의사항
```

## 권장 Git 절차

다음 명령은 예시이며, 실제로 변경한 파일만 명시적으로 추가합니다.

```sh
git status --short
git diff --check
git diff

git add <변경한 파일>
git commit -m "release: v1.1 회원 및 프로필 보안 강화"

git status --short
git show --stat --oneline HEAD
```

`git add .`는 관계없는 사용자 변경이나 비밀 파일까지 포함할 수 있으므로
사용하지 않습니다.

## 커밋 전 필수 점검

- [ ] 변경 내용이 새 버전의 범위와 일치한다.
- [ ] 애플리케이션 테스트가 모두 통과한다.
- [ ] 데이터베이스 변경에 마이그레이션 또는 호환 방안이 있다.
- [ ] `README.md`, `SECURITY_CHANGELOG.md`, `SECURITY_REVIEW.md`, `VERSIONING.md`의 버전이 같다.
- [ ] 보안 체크리스트와 변경 이력이 갱신되었다.
- [ ] 비밀번호, 세션 비밀키, API 토큰 및 개인정보가 포함되지 않았다.
- [ ] 실제 DB, 백업, 캐시 및 임시 파일이 새로 포함되지 않았다.
- [ ] `git diff --check`가 통과한다.
- [ ] 버전과 관련 없는 사용자 변경은 스테이징하지 않았다.

## 커밋 금지 조건

다음 중 하나라도 해당하면 버전 커밋을 만들지 않습니다.

- 테스트 실패 또는 테스트 미실행
- 비밀정보나 개인정보 포함 가능성
- 마이그레이션 없는 비호환 스키마 변경
- 버전 문서 간 버전 불일치
- 변경 범위가 불명확하거나 관계없는 파일이 스테이징됨
- 사용자가 커밋하지 말라고 요청함

## 커밋 이후

- 원격 저장소로의 Push는 별도 작업입니다.
- 버전 커밋을 만들었다는 이유만으로 자동 Push하지 않습니다.
- Git 태그와 GitHub Release도 사용자가 요청하거나 프로젝트 정책이 추가된
  경우에만 생성합니다.
- 배포가 필요한 경우 배포 환경의 비밀키, HTTPS, 데이터베이스 마이그레이션 및
  롤백 계획을 별도로 확인합니다.
